{
  "name": "quarto-dbml",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --target=node18 --external:@resvg/resvg-js --outfile=_extensions/quarto-dbml/dbml-render.js && esbuild src/client.js --bundle --platform=browser --target=es2018 --minify --outfile=_extensions/quarto-dbml/dbml-client.js",
    "dev": "quarto render example/"
  },
  "dependencies": {
    "@dbml/core": "^3.0.0",
    "@resvg/resvg-js": "^2.6.2",
    "svg-pan-zoom": "^3.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
